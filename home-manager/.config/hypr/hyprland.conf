# This is an example Hyprland config file.
#
# Refer to the wiki for more information.

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,1920x1200@60,0x0,1


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = waybar & slack & google-chrome-stable & emacsclient -c -a "emacs"
exec-once = swww init & swww img ~/.local/share/wallpapers/wallpaper.jpg
exec-once = swayidle -w timeout 300 'swaylock -C ~/.config/swaylock/config' timeout 600 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
#env = XCURSOR_SIZE,18

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = false
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 5
    border_size = 1
    col.active_border = rgba(33ccffee)
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 0

    # blur {
    #     enabled = true
    #     size = 3
    #     passes = 1
    # }

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = true

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
#windowrulev2 = float,class:^(pavucontrol)$,title:^(pavucontrol)$

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

windowrulev2=size 40%,title:^(Volume Control)$
windowrulev2=float,title:^(Volume Control)$
#windowrulev2=center 1,title:^(Volume Control)$

windowrulev2=size 40%,title:^(Create Directory)$
windowrulev2=float,title:^(Create Directory)$
#windowrulev2=center 1,title:^(Create Directory)$

windowrulev2=size 40%,title:^(Bluetooth)$
windowrulev2=float,title:^(Bluetooth)$
#windowrulev2=center 1,title:^(Bluetooth)$

windowrule= workspace 1,title:(.*)(Slack)$
windowrule= workspace 1,title:(.*)(Meet)$
windowrule= workspace 2,title:(.*)(Chrome)$
windowrule= workspace 3,title:(.*)(/Emacs/)$
windowrulev2 = opacity 0.8 0.8,title:(.*)(/Emacs/)$

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# SUPER L - Locks immediately, SUPERSHIFT L Turns monitors off (while locked)
bind = $mainMod, L, exec, swaylock -C ~/.config/swaylock/config
bindl = $mainMod SHIFT, L, exec, sleep 1 && hyprctl dispatch dpms off
bindl=,switch:off:Lid Switch, exec, swaylock -C ~/.config/swaylock/config

# Screenshot
bind = $mainMod, P, exec, grim -g "$(slurp)" - | swappy -f -

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, alacritty
bind = $mainMod SHIFT, Return, exec, google-chrome-stable
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, F, exec, thunar
bind = $mainMod, V, togglefloating,
bind = $mainMod, Space, exec, fuzzel
bind = $mainMod, K, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:273, movewindow
bindm = $mainMod, mouse:272, resizewindow

# Brightnessctl:
bind =, XF86MonBrightnessDown, exec, brightnessctl set 10%-
bind =, XF86MonBrightnessUp, exec, brightnessctl set 10%+ 

# Volume up/down/mute:
bind =, XF86AudioRaiseVolume, exec, amixer set Master 5%+
bind =, XF86AudioLowerVolume, exec, amixer set Master 5%-
bind =, XF86AudioMute, exec, exec, amixer set Master toggle